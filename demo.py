from EasyTrainerCore import EasyTrain

if __name__ == "__main__":
    # Start EasyTrainer, this will automatically load the existing model
    # or download the model from torch-hub and start the training
    EasyTrain.start()
